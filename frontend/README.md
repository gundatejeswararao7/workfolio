# Loopline frontend

React + TypeScript + Vite frontend for the Loopline unified work network.

```bash
npm install
npm run dev
npm run build
npm run lint
```

The Vite dev server proxies `/api` requests to `http://localhost:8000`.